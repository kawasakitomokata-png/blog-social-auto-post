name: RSS新記事チェック＆自動投稿

on:
  schedule:
    # JST 23:00 = UTC 14:00 ★チェックのみ（翌日8・12・17時に投稿）
    - cron: '0 14 * * *'
    # JST  9:00 = UTC 00:00 ★チェック＋投稿
    - cron: '0 0 * * *'
    # JST 12:00 = UTC  3:00 ★チェック＋投稿
    - cron: '0 3 * * *'
    # JST 17:00 = UTC  8:00 ★チェック＋投稿
    - cron: '0 8 * * *'
  workflow_dispatch:

concurrency:
  group: check-and-post
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  check-and-post:
    runs-on: ubuntu-latest
    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Pythonのセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 依存パッケージをインストールする
        run: pip install -r requirements.txt

      - name: 23時チェックかどうか判定
        run: |
          if [ "${{ github.event.schedule }}" = "0 14 * * *" ]; then
            echo "SCHEDULE_ONLY=1" >> $GITHUB_ENV
            echo "▶ 23時チェックのみモード（投稿は翌日8・12・17時）"
          else
            echo "SCHEDULE_ONLY=0" >> $GITHUB_ENV
            echo "▶ チェック＋投稿モード"
          fi

      - name: RSS チェック実行（新記事検知＆スケジュール登録）
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          SCHEDULE_ONLY: ${{ env.SCHEDULE_ONLY }}
        run: python check_new_post.py

      - name: 変更があればコミット＆プッシュ
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git reset --mixed origin/main
          git add scheduled_posts.json posted_urls.txt || true
          if git diff --cached --quiet; then
            echo "変更なし、スキップ"
          else
            git commit -m "chore: 自動投稿をスケジュール登録 [skip ci]"
            git push --force-with-lease
          fi

      - name: 投稿送信（21時チェックのみの場合はスキップ）
        if: env.SCHEDULE_ONLY != '1'
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_TOKEN_SECRET: ${{ secrets.X_ACCESS_TOKEN_SECRET }}
          THREADS_ACCESS_TOKEN: ${{ secrets.THREADS_ACCESS_TOKEN }}
          THREADS_USER_ID: ${{ secrets.THREADS_USER_ID }}
        run: python send_posts.py

      - name: 送信済みフラグをコミット＆プッシュ（21時はスキップ）
        if: env.SCHEDULE_ONLY != '1'
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git reset --mixed origin/main
          git add scheduled_posts.json || true
          if git diff --cached --quiet; then
            echo "変更なし、スキップ"
          else
            git commit -m "chore: 送信済みフラグを更新 [skip ci]"
            git push --force-with-lease
          fi
